# PRD · StelLive 팬덤 애널리틱스 대시보드

**작성일** 2026-08-12
**상태** 승인 대기 → 구현 착수
**기획** Opus / **구현** Sonnet

---

## 1. 배경

`youtube_analyze_all/`에 스텔라이브를 소재로 한 데이터 분석 프로젝트 8개가 이미 완성돼 있다.
각 프로젝트는 `collect.py → analyze.py → build_site.py` 파이프라인으로 CSV·SQLite·PNG 차트·
정적 HTML을 만든다.

현재 구조의 한계는 두 가지다.

1. **수치가 늙는다.** 수집 시점(2026-08-03~07) 스냅샷에 고정돼 있고, 갱신하려면 사람이
   로컬에서 스크립트를 순서대로 돌려야 한다. 특히 프로젝트 8(동시시청자)은 소급 수집이
   불가능해서 PC가 꺼진 동안의 데이터가 영구 공백이 된다.
2. **응집력이 없다.** 8개 사이트가 각자 떠 있어서, 보는 사람이 전체 서사를 조립해야 한다.

## 2. 목표

**데이터 분석가(DA) 채용 심사자가 30초 안에 "이 사람이 무엇을 발견했는지"를 파악하고,
관심이 생기면 직접 데이터를 만져볼 수 있는 대시보드를 만든다.**

### 성공 기준

| 지표 | 기준 |
|---|---|
| 랜딩 첫 화면 | 스크롤 없이 핵심 인사이트 3개가 보인다 |
| 데이터 신선도 | 자동 갱신으로 최신 수치 유지, 갱신 시각이 화면에 표시된다 |
| 인터랙션 | 멤버·유닛·기간 필터가 모든 주요 차트에 걸린다 |
| 재현성 | 저장소를 clone한 사람이 문서만 보고 파이프라인 전체를 돌릴 수 있다 |

### 비목표

- 실시간 스트리밍 대시보드 (분 단위 갱신 불필요)
- 로그인·개인화·사용자 데이터 저장
- 모바일 앱

## 3. 타깃

**주 사용자: DA 직무 채용 심사자.** 기술 스택 자체보다 *어떤 질문을 던졌고, 어떻게 검증했고,
무엇을 결론지었는지*를 본다. 따라서:

- 결론을 먼저, 방법론은 그 뒤에 배치한다
- 데이터 함정·표본 편향을 숨기지 않고 명시하는 것이 감점이 아니라 가점이다
- SQL과 pandas의 흔적이 보여야 한다 → **분석 파이프라인은 Python으로 유지**한다

## 4. 범위

### 앱에 포함 (갱신이 필요한 프로젝트)

| # | 프로젝트 | 갱신 주기 | 근거 |
|---|---|---|---|
| 01 | 멤버 채널 성과 | 일 1회 | 구독자·조회수가 매일 변함 |
| 02 | 커버곡 랭킹 | 주 1회 | 조회수 누적은 느리고 `search.list` 쿼터가 큼 |
| 03 | 치지직 방송 패턴 | 일 1회 | 방송이 매일 쌓임 |
| 04 | 키리누키 생태계 | 주 1회 | 클립 생성이 느리고 검색 쿼터가 큼 |
| 05 | 댓글 여론 | 주 1회 | 재수집 비용이 크고 여론 변화가 느림 |
| 06 | 경쟁사 비교 | 일 1회 | 비교군 지표가 매일 변함 |
| 08 | 동시시청자 | **10분** | 소급 수집 불가 — 놓치면 영구 공백 |

### 앱에서 제외 (정적 유지)

**07 시장 분석** — 리서치사 시장 규모, 인수합병 이력, 정부 통계 등 **외부 리서치 기반**이라
자동 갱신이 불가능하다. 기존 정적 페이지를 그대로 두고 허브에서 링크만 건다.
억지로 앱에 넣으면 "갱신되는 것처럼 보이지만 실제로는 고정된" 최악의 상태가 된다.

### Phase 2 (별도 진행)

09 DART 재무 / 10 호요버스 / 11 팬 커머스 — 아직 미구현. 키 발급 및 구현 후 같은 규격으로
허브에 편입한다. **기존 프로젝트와 지표·서사가 겹치지 않게** 각각 독립된 질문을 담당한다.

## 5. 정보 구조

```
/                       허브 — 핵심 인사이트 3개 + 프로젝트 카드 8장 + 갱신 시각
/projects/01-channel    멤버 채널 성과
/projects/02-cover      커버곡 랭킹
/projects/03-stream     방송 패턴
/projects/04-kirinuki   키리누키 생태계
/projects/05-sentiment  댓글 여론
/projects/06-competitor 경쟁사 비교
/projects/08-live       동시시청자          ← 갱신 빈도 최고, 허브에서 강조
/methodology            수집 방법·데이터 함정·표본 한계 (신뢰도 담보 페이지)
```

07은 기존 정적 HTML로 외부 링크 처리한다.

### 랜딩 우선순위

1. **발견한 인사이트 3개** — 수치와 함께 한 문장씩. 예: "팔로워 규모 1위와 팬덤 밀도 1위는
   다른 사람이다"
2. 프로젝트 카드 그리드 — 각 카드에 대표 지표 1개
3. 데이터 신선도 배지 — 마지막 갱신 시각, 수집 소스 수

## 6. 기능 요구사항

### FR-1 필터 (전역)
멤버(11명) · 유닛(EVERYS/UNIVERSE/CLICHE/창립자) · 기간 필터. 선택 상태는 URL 쿼리에
반영해 공유 가능해야 한다.

### FR-2 드릴다운
차트 요소 클릭 → 해당 멤버 상세 패널. 상세에는 그 멤버의 전 프로젝트 지표가 모인다.

### FR-3 정렬 가능한 표
모든 랭킹 표는 열 클릭 정렬. 현재 정렬 기준이 시각적으로 드러나야 한다.

### FR-4 데이터 신선도 표기
각 페이지 상단에 해당 데이터의 `fetched_at`. 기준보다 오래되면 경고 배지.

### FR-5 커버리지 가드 계승
프로젝트 8의 커버리지 가드(관측 24시간·세션 5건 미만이면 결론 보류)를 UI에도 반영한다.
데이터가 얇을 때 그럴듯한 차트를 보여주지 않는다.

### FR-6 방법론 페이지
치지직 stale viewer 함정, Enka 표본 편향, YouTube 쿼터 제약을 명시한다.
**이것이 DA 포트폴리오의 차별점이다.**

## 7. 데이터 계약

Python이 수집·분석하고 JSON을 뱉는다. Next.js는 읽어서 렌더만 한다. 역할을 섞지 않는다.

```
youtube_analyze_all/<project>/site/data.json   ← 기존 analyze.py가 이미 생성
                    ↓ (빌드 시 복사/임포트)
app/src/data/<project>.json
```

### 공통 스키마 요구

모든 `data.json`은 최소한 다음을 포함한다.

```jsonc
{
  "meta": { "fetched_at": "ISO8601", "source": "...", "n_*": 0 },
  "members": [ { "name_ko": "...", "name_en": "...", "unit": "...", /* 지표 */ } ]
}
```

`name_en`을 조인 키로 쓴다. `common/config.py`의 로스터가 유일한 정본이다.

**구현 시 확인 필요:** 기존 7개의 `data.json`이 이 규격을 이미 만족하는지 검사하고,
부족하면 각 `analyze.py`의 `build_outputs()`에 필드를 추가한다. Next.js 쪽에서 억지로
맞추지 않는다.

## 8. 기술 스택

| 항목 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Next.js (App Router) + TypeScript | `output: 'export'` 정적 빌드 |
| 디자인 | **Montage 토큰 기반 자체 컴포넌트** | 아래 결정 사항 참고 |
| 차트 | 직접 구현한 SVG 컴포넌트 | 외부 차트 라이브러리 미사용 |
| 분석 | Python 3.12 + pandas + SQLite | 기존 유지 |
| 배포 | 없음 — 로컬 실행 전용 | git/GitHub 미사용 결정 (2026-08-12) |

### 결정: Montage는 토큰만 사용한다

`@wanteddev/wds`는 공개 npm에 없고 **GitHub Packages에만 배포**된다
(`publishConfig.registry: https://npm.pkg.github.com/`). 무인증 접근은 401이며, 원티드랩
조직 패키지가 외부에 열려 있는지도 확인 불가다. 토큰을 발급해도 403이 날 수 있다.

따라서 **`common/montage.py`에 추출해둔 실제 토큰 값**(atomic 팔레트, light/dark 시맨틱,
spacing, radius, shadow, Pretendard 스택)으로 컴포넌트를 직접 구현한다. 외부 의존과 인증이
없어 CI가 단순해지고, 시각적 결과물은 사실상 동일하다.

토큰 출처는 `github.com/wanteddev/montage-web` (MIT)이며 README에 명시한다.

## 9. 자동화 (Windows 작업 스케줄러 · 로컬)

> **결정 변경 (2026-08-12)** — 사용자가 git/GitHub을 사용하지 않기로 했다. 따라서
> GitHub Actions 기반 자동화는 채택하지 않는다. 동일한 목적을 Windows 작업 스케줄러로
> 로컬에서 달성한다. 저장소·시크릿·과금 이슈가 모두 사라지는 대신, **PC가 켜져 있어야
> 수집이 돈다**는 제약이 남는다.

| 작업 | 주기 | 하는 일 |
|---|---|---|
| `collect-live` | 10분 | `08/collect.py --once` |
| `collect-daily` | 매일 1회 | 01·03·06 수집 → 분석 |
| `collect-weekly-a` | 주 1회 (예: 화) | 02 수집 → 분석 |
| `collect-weekly-b` | 주 1회 (예: 금) | 04·05 수집 → 분석 |
| `build` | 수동 | Next.js 정적 빌드 |

### 제약

- **PC가 꺼져 있으면 그 구간은 영구 공백이다.** 프로젝트 8은 소급 수집이 불가능하다.
  절전 모드를 해제해두는 것이 전제다.
- 작업 스케줄러는 PC가 꺼져 있던 시간의 작업을 놓친다. "놓친 작업 즉시 실행" 옵션을 켜도
  10분 간격 시계열의 공백은 메워지지 않는다.
- **YouTube 쿼터**: 일 10,000 units + `search.list` 별도 100회/일. 02와 04가 검색을 쓰므로
  **같은 날 실행하지 않는다.** 요일을 분리한다 (위 표의 weekly-a / weekly-b).
- API 키는 사용자 환경변수(`setx`)로 주입한다. 저장소가 없으므로 시크릿 관리 이슈는 없다.
- 스케줄러 등록은 시스템 설정 변경이므로 **사용자 승인 후에만** 수행한다.

### 폐기된 대안

GitHub Actions — cron 정시 미보장(10분 설정 시 실제 10~20분), 비공개 저장소는 월 2,000분
무료 한도를 초과해 과금(10분 간격 시 월 4,000~8,000분 소요), Pages도 비공개는 유료.
무엇보다 사용자가 git 사용을 원하지 않는다.

## 10. 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| 치지직 비공식 API 차단 | 03·08 수집 중단 | 재시도·실패 로깅 이미 구현. 실패 시 마지막 성공 데이터 유지 |
| 데이터 얇을 때 오해 유발 | 신뢰도 하락 | FR-5 커버리지 가드 |
| Actions cron 지연 | 시계열 간격 불균일 | 간격을 가정하지 않는 집계 로직 |
| 데이터 커밋으로 저장소 비대 | clone 느려짐 | 원본 CSV는 append-only, 주기적 압축 검토 |

## 11. 마일스톤

| 단계 | 산출물 | 상태 |
|---|---|---|
| M1 | Next.js 스캐폴드 + Montage 토큰 시스템 + 허브 골격 | ✅ |
| M2 | 데이터 계약 검증 및 `data.json` 규격 보정 | ✅ |
| M3 | 프로젝트 페이지 7개 + 전역 필터/드릴다운 | ✅ |
| M4 | 방법론 페이지 + 신선도 배지 + 커버리지 가드 UI | ✅ |
| M5 | 작업 스케줄러 등록 스크립트 | ✅ 작성 완료 · **등록은 사용자가 `-Apply` 로 실행** |
| Phase 2 | 09 DART 재무 · 10 호요버스 · 11 팬 커머스 + 앱 편입 | ✅ |

### 완료 시점 상태 (2026-08-12)

- 라우트 12개 정적 export, eslint 0건
- 프로젝트 11개 중 10개가 실데이터 연결, 07만 정적 외부 링크
- `/methodology` 에 실제 발생한 함정 9건 기록

### Phase 2 진행 중 확정된 사실

- **팬딩은 DART에 단 한 건도 제출한 적이 없다.** 파싱 한계가 아니라 부존재다
  (1999~2026 전 구간, 필터 없이 조회). 팬 커머스의 재무 층위는 팬딩이 아니라
  패러블엔터테인먼트·샌드박스네트워크로 대체했다.
- **스텔라이브·브레이브그룹은 DART 기업코드 마스터(118,701개)에 없다.**
- `fnlttSinglAcntAll.json` 은 정기보고서만 파싱한다. 감사보고서만 내는 비상장사는
  `document.xml` 원문(ZIP → DART 자체 DTD)을 직접 파싱해야 재무를 얻을 수 있다.
- **텀블벅은 `robots.txt` 가 `/api/` 를 막고 모금 수치가 그 API에만 있다.**
  헤드리스 브라우저로 우회하지 않고, 수집 불가를 `_meta.json` 에 기록한 뒤
  출처를 명시한 정적 데이터로 대체했다.

---

## 부록 · 확정된 인터뷰 결과

- **타깃**: 데이터 분석가(DA)
- **랜딩 우선순위**: 발견한 인사이트
- **인터랙션**: 필터 + 드릴다운
- **기존 Python 파이프라인**: 유지, JSON 공급원 역할
- **배포**: 로컬 전용, 새 디렉토리(`app/`)에 생성. **git/GitHub 미사용** (2026-08-12 결정)
- **자동화**: GitHub Actions → Windows 작업 스케줄러로 대체
- **앱 범위**: 갱신이 필요한 프로젝트만. 불필요한 것은 기존 상태 유지, 서로 겹치지 않게
- **Montage**: 토큰 기반 자체 구현
