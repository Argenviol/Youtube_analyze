pandas>=2.0
numpy>=1.24
matplotlib>=3.7
requests>=2.28
tabulate>=0.9

# 09 감사보고서 원문(document.xml) 파싱 — collect.py 최상위 import 라 없으면 즉시 죽는다.
beautifulsoup4>=4.12
# 10 Google Play 리뷰 수집 — fetch_reviews() 에서 import.
google-play-scraper>=1.2

# 10 Google Trends — try_google_trends() 에서 import.
#
# 이전에는 일부러 빼 뒀다. 그 결과 매 실행이 ModuleNotFoundError 로 끝났고,
# trends_status.json 에는 "호출 안 함"만 쌓였다. **시도하지 않은 것과 시도했다가
# 막힌 것은 다른 사실**인데, 리포트는 후자(429)라고 적고 있었다 — 기록과 문장이
# 어긋나 있었다는 뜻이다.
#
# 그래서 설치해서 매번 실제로 호출한다. 성공하면 쓰고, 429 든 다른 이유든 실패하면
# 그 실패를 trends_status.json 에 그대로 남긴다. 실패해도 파이프라인은 멈추지 않는다
# (try_google_trends 가 예외를 잡아 상태만 기록한다).
pytrends>=4.9

# 결과물 통합 리포트(scripts/build_unified.py) 가 REPORT.md 를 HTML 로 변환할 때 쓴다.
markdown>=3.5
