pandas>=2.0
numpy>=1.24
matplotlib>=3.7
requests>=2.28
tabulate>=0.9

# 09 감사보고서 원문(document.xml) 파싱 — collect.py 최상위 import 라 없으면 즉시 죽는다.
beautifulsoup4>=4.12
# 10 Google Play 리뷰 수집 — fetch_reviews() 에서 import.
google-play-scraper>=1.2

# pytrends 는 일부러 넣지 않는다. Google Trends 는 매 호출 429 로 실패해 설계에서
# 드롭했고(10_hoyoverse/collect.py 상단 주석 참고), try_google_trends() 의
# except Exception 이 ImportError 까지 잡아 실행을 막지 않는다.
